apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-scripts
  namespace: cloudcart
data:
  01-init.sql: |
    USE cloudcart;
    CREATE TABLE IF NOT EXISTS users (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        email VARCHAR(150) UNIQUE NOT NULL,
        password_hash VARCHAR(255) NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        INDEX idx_email (email)
    );
    CREATE TABLE IF NOT EXISTS products (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(200) NOT NULL,
        description TEXT,
        price DECIMAL(10, 2) NOT NULL,
        stock INT DEFAULT 0,
        image_url VARCHAR(500) DEFAULT 'https://via.placeholder.com/300',
        category VARCHAR(100),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
    CREATE TABLE IF NOT EXISTS cart_items (
        id INT AUTO_INCREMENT PRIMARY KEY,
        user_id INT NOT NULL,
        product_id INT NOT NULL,
        quantity INT DEFAULT 1,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
        FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
        UNIQUE KEY unique_user_product (user_id, product_id)
    );
    CREATE TABLE IF NOT EXISTS orders (
        id INT AUTO_INCREMENT PRIMARY KEY,
        user_id INT NOT NULL,
        total_amount DECIMAL(10, 2) NOT NULL,
        status ENUM('pending', 'confirmed', 'shipped', 'delivered', 'cancelled') DEFAULT 'pending',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (user_id) REFERENCES users(id)
    );
    CREATE TABLE IF NOT EXISTS order_items (
        id INT AUTO_INCREMENT PRIMARY KEY,
        order_id INT NOT NULL,
        product_id INT NOT NULL,
        quantity INT NOT NULL,
        price_at_purchase DECIMAL(10, 2) NOT NULL,
        FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,
        FOREIGN KEY (product_id) REFERENCES products(id)
    );
  02-seed.sql: |
    USE cloudcart;
    INSERT IGNORE INTO products (name, description, price, stock, image_url, category) VALUES
    ('iPhone 15 Pro', 'Apple iPhone 15 Pro with A17 Pro chip', 99999.00, 50, 'https://via.placeholder.com/300?text=iPhone', 'Electronics'),
    ('Samsung Galaxy S24', 'Samsung Galaxy S24 Ultra', 79999.00, 75, 'https://via.placeholder.com/300?text=Samsung', 'Electronics'),
    ('MacBook Air M3', 'Apple MacBook Air with M3 chip', 114900.00, 30, 'https://via.placeholder.com/300?text=MacBook', 'Computers'),
    ('Sony WH-1000XM5', 'Noise canceling headphones', 24999.00, 100, 'https://via.placeholder.com/300?text=Sony', 'Electronics'),
    ('Nike Air Max 270', 'Running shoes', 9999.00, 200, 'https://via.placeholder.com/300?text=Nike', 'Footwear');